version: "3.8"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    depends_on:
      - db
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: t10102001
      MYSQL_DATABASE: vinafoodco
    volumes:
      - mysql_data:/var/lib/mysql
volumes:
  mysql_data:
